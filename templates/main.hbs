<!DOCTYPE html>
<html lang="en">
  <head>
    {{>head}}
    {{>styles}}
  </head>
  <body>
    <div class="container index-0">
      {{>nav}}
      {{>intro}}
      {{>grid_feeling}}
      {{>grid_because}}
      {{>grid_better}}
      {{>mad-libs}}
      {{>scripts}}
    </div>
  </body>
</html>
